/**
 * @mainpage Sum Function Documentation
 *
 * This project provides a simple function to compute the sum of two floating-point numbers.
 * The function is designed to be efficient and easy to use.
 *
 * @section intro Introduction
 * The `sum` function is a basic arithmetic operation that adds two double-precision floating-point numbers.
 * It is useful in various applications where precise addition of real numbers is required.
 *
 * @section detailed Detailed Description
 * For more details about the `sum` function, see @ref sum.
 *
 * @section example Example Usage
 * @code
 * double result = sum(3.5, 2.5); // result will be 6.0
 * @endcode
 */

/**
 * @brief Function to compute the sum of two floating-point numbers.
 *
 * This function takes two floating-point numbers and returns their sum.
 *
 * @param a The first floating-point number.
 * @param b The second floating-point number.
 * @return The sum of the two floating-point numbers.
 */
double sum(double, double);
